// Cypress E2E support file
// Add custom commands and global configuration here

// Suppress uncaught exceptions from the app (e.g. from deferred rendering)
Cypress.on("uncaught:exception", () => false);
